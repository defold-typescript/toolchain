# Unnarrowable collision

Both example roots hold `src/shared.ts`, and this page quotes nothing else, so no
fence names a root the collision could be narrowed to:

```ts title="src/shared.ts"
export const shared = "one";
```
