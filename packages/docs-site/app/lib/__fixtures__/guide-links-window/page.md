# Window links

The explicit window, bare and anchored:
[a tracked window](/api/defold-1.12.4/go?since=defold-1.12.0) and
[the same window at a symbol](/api/defold-1.12.4/go?since=defold-1.12.0#create).

A window whose lower bound is no longer tracked:
[a retired bound](/api/defold-1.12.4/go?since=defold-9.9.9).

A window on a route the site does not emit:
[a retired page](/api/defold-1.12.4/retired?since=defold-1.12.0).
