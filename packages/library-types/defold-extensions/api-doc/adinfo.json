{
  "info": {
    "namespace": "adinfo",
    "brief": "Provides functionality to get the advertising id and tracking status. Supported on iOS and Android. [icon:ios] [icon:android]",
    "description": "Provides functionality to get the advertising id and tracking status. Supported on iOS and Android. [icon:ios] [icon:android]"
  },
  "elements": [
    {
      "type": "FUNCTION",
      "name": "adinfo.get",
      "description": "Get a table with advertising information. [icon:attention] function returns nil if values do not ready",
      "parameters": [
        {
          "name": "callback",
          "doc": "The function to call when advertising information is available. On iOS 14 and above\nthis will trigger a tracking request popup.",
          "types": [
            "function"
          ]
        }
      ],
      "returnvalues": [],
      "examples": "<div class=\"codehilite\"><pre><code class=\"language-lua\">function init(self)\n    adinfo.get(function(self, info)\n      print(info.ad_ident, info.ad_tracking_enabled)\n    end)\nend</code></pre></div>"
    }
  ]
}
