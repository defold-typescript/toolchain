{
  "string.byte": {
    "signatures": [
      "string.byte(s: string, i?: number): number",
      "string.byte(s: string, i?: number, j?: number): LuaMultiReturn<number[]>"
    ]
  },
  "string.char": {
    "signatures": ["string.char(...args: number[]): string"]
  },
  "string.dump": {
    "signatures": ["string.dump(func: Function): string"]
  },
  "string.find": {
    "signatures": [
      "string.find(s: string, pattern: string, init?: number, plain?: boolean): LuaMultiReturn<[number, number, ...string[]] | []>"
    ]
  },
  "string.format": {
    "signatures": ["string.format(formatstring: string, ...args: any[]): string"]
  },
  "string.gmatch": {
    "signatures": [
      "string.gmatch(s: string, pattern: string): LuaIterable<LuaMultiReturn<string[]>>"
    ]
  },
  "string.gsub": {
    "signatures": [
      "string.gsub(s: string, pattern: string, repl: string | Record<string, string> | ((...matches: string[]) => string), n?: number): LuaMultiReturn<[string, number]>"
    ]
  },
  "string.len": {
    "signatures": ["string.len(s: string): number"]
  },
  "string.lower": {
    "signatures": ["string.lower(s: string): string"]
  },
  "string.match": {
    "signatures": [
      "string.match(s: string, pattern: string, init?: number): LuaMultiReturn<string[]>"
    ]
  },
  "string.rep": {
    "signatures": ["string.rep(s: string, n: number): string"]
  },
  "string.reverse": {
    "signatures": ["string.reverse(s: string): string"]
  },
  "string.sub": {
    "signatures": ["string.sub(s: string, i: number, j?: number): string"]
  },
  "string.upper": {
    "signatures": ["string.upper(s: string): string"]
  }
}
