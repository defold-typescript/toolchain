{
  "msg.post": {
    "signatures": [
      "msg.post<K extends (keyof M & string) | MessageId | (string & {}), M extends object = Record<never, never>>(receiver: SceneAddress | Hash | ReceiverUrl<M>, message_id: K, message?: K extends BuiltinMessageId ? MessagePayload<K> : K extends keyof M ? M[K] : MessagePayload<K>): void",
      "msg.post(receiver: SceneAddress | Url | Hash, message_id: Hash, message?: Record<string | number, unknown>): void"
    ],
    "docs": [
      "Post a message to a receiving URL. The most common case is to send messages to a component. If the component part of the receiver is omitted, the message is broadcast to all components in the game object. The following receiver shorthands are available:\n\n- `\".\"` the current game object\n\n- `\"#\"` the current component\n\nThere is a 2 kilobyte limit to the message parameter table size.",
      null
    ]
  },
  "msg.url": {
    "signatures": [
      "msg.url(): Url",
      "msg.url(urlstring: SceneAddress | undefined): Url",
      "msg.url(socket: string | Hash | undefined, path: string | Hash, fragment: string | Hash): Url",
      "msg.url<M extends object>(): ReceiverUrl<M>",
      "msg.url<M extends object>(urlstring: SceneAddress | undefined): ReceiverUrl<M>",
      "msg.url<M extends object>(socket: string | Hash | undefined, path: string | Hash, fragment: string | Hash): ReceiverUrl<M>"
    ],
    "docs": [
      "Construct a URL. A URL is `[socket:][path][#fragment]`.",
      null,
      null,
      null,
      null,
      null
    ]
  }
}
