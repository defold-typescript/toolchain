// The extension slots OPTIONAL_SLOT_CORRECTIONS promotes: each is documented as
// omissible by the extension's own prose while the ref-doc metadata still marks
// it required. The omitted call a user would copy out of the docs must compile
// against the declarations resolve emits.

// iap.buy — "optional parameters as properties".
iap.buy("sword");
